<%= turbo_stream.close_modal %>

<%= turbo_stream.update "boards" do %>
  <%= render partial: "boards", locals: { boards: @boards } %>
<% end %>

<%= turbo_stream.redirect(account_board_path(@board)) %>
